<%= form_for([@post, @comment]) do |form| %>
  <%= form.text_area :comment, class: "bg-gray-100 ml-20 mt-8 mr-2 rounded border border-gray-400 leading-normal resize-none w-1/2 h-64 py-2 px-3 font-medium focus:outline-none focus:bg-white" %>
  <%= form.submit "編集", class: "bg-white mt-64 ml-2 absolute text-gray-700 font-medium py-1 px-4 border border-gray-400 rounded-lg tracking-wide hover:bg-gray-100" %>
<% end %>